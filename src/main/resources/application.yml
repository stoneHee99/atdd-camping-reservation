spring:
  application:
    name: camping-reservation-legacy

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: true


server:
  port: 8080

logging:
  level:
    com.camping: DEBUG